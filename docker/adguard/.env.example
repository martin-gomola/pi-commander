# AdGuard Home Configuration

# DNS Ports (external -> internal 53)
ADGUARD_DNS_TCP=53
ADGUARD_DNS_UDP=53

# Web Interface Ports
ADGUARD_SETUP_PORT=3004    # Initial setup wizard (disable after first run)
ADGUARD_WEB_PORT=3001      # Admin panel

# Secure DNS Ports (optional)
ADGUARD_HTTPS_PORT=8443    # DNS-over-HTTPS
ADGUARD_DOT_PORT=853       # DNS-over-TLS

# Timezone
TZ=Europe/Bratislava
