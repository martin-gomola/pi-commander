name: Deploy Streamlit to Oracle Cloud

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H ${{ secrets.HOST }} >> ~/.ssh/known_hosts

      - name: SSH into Oracle VM and prepare deployment directory
        run: |
          ssh ${{ secrets.USERNAME }}@${{ secrets.HOST }} << EOF
            mkdir -p /home/ubuntu/cloud
            chown ubuntu:ubuntu /home/ubuntu/cloud
            chmod 700 /home/ubuntu/cloud
            echo "Deployment directory prepared."
          EOF

      - name: Copy application files to Oracle Cloud VM
        run: |
          scp -r ./divvy_tax ${{ secrets.USERNAME }}@${{ secrets.HOST }}:/home/ubuntu/cloud/

      - name: Copy Nginx configuration
        run: |
          scp ${{ github.workspace }}/nginx/streamlit.conf ${{ secrets.USERNAME }}@${{ secrets.HOST }}:/tmp/streamlit.conf

      - name: Copy Systemd service file
        run: |
          scp ${{ github.workspace }}/systemd/streamlit.service ${{ secrets.USERNAME }}@${{ secrets.HOST }}:/tmp/streamlit.service

      - name: Check if static directory exists and is a directory
        run: |
          ssh ${{ secrets.USERNAME }}@${{ secrets.HOST }} "[[ -d /var/www/html/static ]] && echo 'directory' || echo 'file'"

      - name: Copy static files
        run: |
          scp ${{ github.workspace }}/static/index.html ${{ secrets.USERNAME }}@${{ secrets.HOST }}:/var/www/html/static
  
      - name: SSH into Oracle VM and deploy app
        run: |
          ssh ${{ secrets.USERNAME }}@${{ secrets.HOST }} << 'EOF'
            set -e  # Stop execution on any error

            # Remove existing symlink
            sudo rm -f /etc/nginx/sites-enabled/streamlit.conf 
            
            # Install dependencies
            cd /home/ubuntu/cloud/divvy_tax
            if [ -f "requirements.txt" ]; then
              pip install -r requirements.txt
            else
              echo "requirements.txt not found!"
              exit 1
            fi

            # Clean up any old virtual environments
            rm -rf venv

            # Create a new virtual environment
            python3 -m venv venv

            # Activate virtual environment and install dependencies
            source venv/bin/activate
            pip install -r requirements.txt

            # Copy Nginx and Systemd files to correct locations
            sudo cp /tmp/streamlit.conf /etc/nginx/sites-available/streamlit.conf
            sudo ln -s /etc/nginx/sites-available/streamlit.conf /etc/nginx/sites-enabled/
            sudo cp /tmp/streamlit.service /etc/systemd/system/streamlit.service

            # Reload systemd and nginx
            sudo systemctl daemon-reload
            sudo systemctl restart nginx
            sudo systemctl restart streamlit

            echo "Deployment completed successfully!"
          EOF