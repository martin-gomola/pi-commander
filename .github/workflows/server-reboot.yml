name: Server Maintenance & Keep Alive

on:
  schedule:
    - cron: '*/30 * * * *'  # Runs every 30 minutes to keep server alive
    - cron: '0 0 * * *'  # Runs daily at 00:00 UTC (1 AM Bratislava time) to update & reboot
  workflow_dispatch:  # Allows manual execution

jobs:
  keep_alive:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Server (road-work.gd)
        run: curl -s --head https://matie.run.place > /dev/null || echo "Ping failed, but continuing."

  update_and_reboot:
    runs-on: ubuntu-latest
    steps:
      - name: Set up SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H ${{ secrets.HOST }} >> ~/.ssh/known_hosts

      - name: Update & Reboot Server
        run: |
          ssh -o StrictHostKeyChecking=no -o LogLevel=ERROR ${{ secrets.USERNAME }}@${{ secrets.HOST }} << 'EOF'
            echo "Starting system update..."
            sudo apt update && sudo apt upgrade -y
            echo "System update complete. Rebooting server..."
            sudo nohup bash -c "sleep 5 && reboot" >/dev/null 2>&1 &
            exit
          EOF
